<?php
    $myInt = 89;
    $myBool = false;
    //Classical if
    if ($myInt > 90) {
        //Here some code
    }
    else if ($myBool == true) {
        //Here some code
    }
    else {
        //Here some code
    }

    //Binary if
    $myVar = ($myInt >= 90) ? 'Katta' : 'Kichik';

    /**
     * AND <-> &&
     * OR <-> ||
     * NOT <-> !
     * ==
     * != => not
     * >=
     * <=
     * >
     * <
     * === - tip va qiymat tengligini solishtirish
     */